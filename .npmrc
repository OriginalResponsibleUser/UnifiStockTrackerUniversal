# Suppress funding messages during install
fund=false

# Use exact versions for better reproducibility
save-exact=false

# Automatically fix vulnerabilities during install
audit-level=moderate

# Reduce noise from deprecated package warnings
loglevel=warn
